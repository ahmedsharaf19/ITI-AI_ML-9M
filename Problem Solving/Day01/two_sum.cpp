class Solution {
public:
    // BruteForce Approach
    /*
    vector<int> twoSum(vector<int>& nums, int target) {
        int firstIndex = -1, secondIndex = -1;
        for( int i = 0; i < nums.size(); i++){
            for ( int j = i + 1; j < nums.size(); j++){
                if (nums[i] + nums[j] == target){
                    firstIndex = i;
                    secondIndex = j;
                    break;
                }
            }
        }
        vector<int> indices = {firstIndex, secondIndex};
        return indices;
    }
    */

    // Optimal Approach
    
    

};
